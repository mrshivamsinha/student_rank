import React from "react";

const Details = () => (
  <div className="p-6">
    <h2 className="text-2xl font-bold">Other Details</h2>
    <p>This tool displays BEU ranks based on registration numbers from internal records.</p>
  </div>
);

export default Details;
